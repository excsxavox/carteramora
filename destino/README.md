# destino — Salidas del job

Reportes generados por el job. Misma jerarquía que `docsmora`:

```
destino/
└── 2026/
    └── 05042026/
        └── cartera05042026b/
            ├── reporte_mora.json
            └── reporte_mora.lis    ← manifiesto (leídos + generados)
```

| Carpeta | Uso |
|---------|-----|
| `docsmora/` | Archivos de **entrada** (cuadro morosidad + TE cartera) |
| `destino/` | Archivos de **salida** (reporte JSON) |
